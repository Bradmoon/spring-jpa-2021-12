spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql=trace

# define datasource
spring.datasource.url=jdbc:mysql://localhost:3306/customer?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=password

# capture the ddl but does not create the tables
#spring.jpa.properties.javax.persistence.schema-generation.scripts.action=create
#spring.jpa.properties.javax.persistence.schema-generation.scripts.create-target=create.sql
#spring.jpa.hibernate.ddl-auto=create
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# have jpa create the database (does not allow for capture of ddl)
spring.jpa.generate-ddl=true
# change none to create  to create the db
spring.jpa.hibernate.ddl-auto=none 
